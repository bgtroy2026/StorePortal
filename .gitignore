# data — never commit raw pulls, the warehouse, or built bundles
raw/
state/
_site/
site/data/
*.sqlite
*.sqlite-wal
*.sqlite-shm

# secrets
.env
*.env
bundle_keys.json

# python
__pycache__/
*.pyc
.venv/

# os / editor
.DS_Store
Thumbs.db

# deploy logs
_deploy/*.log

# deploy token (kept outside the repo; listed here as belt-and-braces)
.storeportal-token

# working files that are not part of the portal (VIP exports and the like) — never publish
potential/
